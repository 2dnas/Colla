import XCTest
@testable import CollaborationNetworkModule

final class CollaborationNetworkModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CollaborationNetworkModule().text, "Hello, World!")
    }
}
